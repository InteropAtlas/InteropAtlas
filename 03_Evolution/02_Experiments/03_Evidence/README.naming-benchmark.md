# Naming Benchmark Evidence

Naming Benchmark 的 durable artifacts：

- `naming-benchmark-proposals.json`：正式候选机器可读归档 / 索引；
- `naming-benchmark-archive.zh-CN.md`：归档规则与当前覆盖；
- 执行事实源与完整 rationale / screening / checkpoint：GitHub Issue #410；
- generation / execution protocol：GitHub Issue #409。

当前 `naming-benchmark-proposals.json` 已覆盖 G0 1–40。后续 arm 应在每个 generation checkpoint 同步更新该文件。
