# 03_Decisions

`03_Decisions` 保存脱离具体施工周期后仍具有独立长期价值的方向与架构决策依据。

适合进入这里的内容应回答：**为什么 InteropAtlas 会采用、放弃或修订某个重要方向 / 架构选择？**

它不是阶段计划、任务清单或迁移日志的归档区。当前正式规则进入 `docs/`；当前实现进入 `02_Runtime/`；只有工作过程价值的材料由 Issue / PR / Git history 保留。

本目录中的历史草案可以保留作为 rationale，但必须以其自身状态理解，不得因位于 `03_Decisions/` 就被误认为当前规范。
