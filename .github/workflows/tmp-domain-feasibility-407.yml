name: Temporary domain feasibility #407
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary workflow disabled after #407 domain feasibility check."
