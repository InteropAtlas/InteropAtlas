name: tmp-rdap-g7-r2
on:
  push:
    branches: [tmp-rdap-g7-r2]
permissions:
  contents: read
jobs:
  rdap:
    runs-on: ubuntu-latest
    steps:
      - name: Query PIR RDAP
        shell: bash
        run: |
          set -u
          endpoint="https://rdap.publicinterestregistry.org/rdap/domain"
          names=(openhand sidepath wayfarer kindling crosscurrent manyhands farside turnkey commonroom newside relaypoint wayglass goodbetween publicside elsepath manyturns sharedway forthside openframe sidecar)
          for n in "${names[@]}"; do
            code=$(curl -sS -o /tmp/rdap.json -w '%{http_code}' "$endpoint/$n.org" || true)
            echo "$n.org $code"
          done
