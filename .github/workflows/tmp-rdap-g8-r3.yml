name: tmp-rdap-g8-r3
on:
  push:
    branches: [tmp-rdap-g8-r3]
permissions:
  contents: read
jobs:
  rdap:
    runs-on: ubuntu-latest
    steps:
      - name: Query PIR RDAP
        shell: bash
        run: |
          set -u
          endpoint="https://rdap.publicinterestregistry.org/rdap/domain"
          names=(ownfold limenwake morrowlens elsehinge elsewake limenraft merepass facetmorrow prismwake sensemere viewmorrow worldfacet sharedverge vergekind knownelse knownmorrow viewkindle relaymere carrymere mosaicmere)
          for n in "${names[@]}"; do
            code=$(curl -sS -o /tmp/rdap.json -w '%{http_code}' "$endpoint/$n.org" || true)
            echo "$n.org $code"
          done
