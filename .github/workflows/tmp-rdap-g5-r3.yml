name: tmp-rdap-g5-r3
on:
  push:
    branches: [tmp-rdap-g5-r3]
permissions:
  contents: read
jobs:
  rdap:
    runs-on: ubuntu-latest
    steps:
      - name: Query PIR RDAP
        shell: bash
        run: |
          set -u
          endpoint="https://rdap.publicinterestregistry.org/rdap/domain"
          names=(commonstrange othermade morrowcommon oddside elsebetter commonsame anothergood sidekind morrowelse publicdifferent turnodd commonfurther elsecommon nearother oneelse differentkind morrowodd commonapart otherwell elseforward)
          for n in "${names[@]}"; do
            code=$(curl -sS -o /tmp/rdap.json -w '%{http_code}' "$endpoint/$n.org" || true)
            echo "$n.org $code"
          done
