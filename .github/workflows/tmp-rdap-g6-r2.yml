name: tmp-rdap-g6-r2
on:
  push:
    branches: [tmp-rdap-g6-r2]
permissions:
  contents: read
jobs:
  rdap:
    runs-on: ubuntu-latest
    steps:
      - name: Query PIR RDAP
        shell: bash
        run: |
          set -u
          endpoint="https://rdap.publicinterestregistry.org/rdap/domain"
          names=(continuance relaywork commonhold perennel everpass carryline everance passagefield custodia throughkeep longcurrent inheritway perena carrycommons relayline everthread continuara handforward lineagework perennum)
          for n in "${names[@]}"; do
            code=$(curl -sS -o /tmp/rdap.json -w '%{http_code}' "$endpoint/$n.org" || true)
            echo "$n.org $code"
          done
