name: tmp-rdap-g3-r3
on:
  push:
    branches: [tmp-rdap-g3-r3]
permissions:
  contents: read
jobs:
  rdap:
    runs-on: ubuntu-latest
    steps:
      - name: Query PIR RDAP
        shell: bash
        run: |
          set -u
          endpoint="https://rdap.publicinterestregistry.org/rdap/domain"
          names=(longwindow thirdlantern borrowedweather sideorchard commonfire elsedoor farhearth manyroads guestriver samelantern openweather secondhearth commonwindow thirdshore foundlantern sidefield borroweddoor longriver manydoors guestweather)
          for n in "${names[@]}"; do
            code=$(curl -sS -o /tmp/rdap.json -w '%{http_code}' "$endpoint/$n.org" || true)
            echo "$n.org $code"
          done
