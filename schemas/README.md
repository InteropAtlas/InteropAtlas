# Schemas

This directory will contain machine-readable validation schemas for InteropAtlas data objects.

The initial source format is YAML, validated using JSON Schema. Schemas themselves are functional project code/configuration and are licensed under **Apache-2.0**, unless explicitly stated otherwise.